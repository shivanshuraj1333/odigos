# Optional: backend URL when running Next dev. Default is http://localhost:8085 (port-forward target).
# In another terminal: kubectl port-forward -n <odigos-ns> svc/odigos-ui 8085:3000
# NEXT_PUBLIC_BACKEND_ORIGIN=http://localhost:8085
